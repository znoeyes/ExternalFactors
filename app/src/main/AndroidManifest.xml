<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.externalfactorsproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GoodOrBadPopupActivity"
            android:theme="@style/AppTheme.Dialog" ></activity>
        <activity android:name=".PlayMusicVideoActivity" />
        <activity
            android:name=".EmotionPopUpActivity"
            android:theme="@style/AppTheme.Dialog" />
        <activity
            android:name=".CameraActivity"
            android:hardwareAccelerated="true" />
        <activity
            android:name=".HeartRatePopUpActivity"
            android:hardwareAccelerated="true"
            android:theme="@style/AppTheme.Dialog" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme.home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>